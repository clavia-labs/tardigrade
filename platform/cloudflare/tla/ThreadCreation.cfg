\* ThreadCreation checks that only an accepted child becomes visible in the actor directory.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Calls <- ModelCalls
  Threads <- ModelThreads
  Target <- ModelTarget
  None = "none"
INVARIANT TypeOK
INVARIANT ReadyHasAccepted
INVARIANT AcceptedHasReservation
INVARIANT PendingOwnsRequest
