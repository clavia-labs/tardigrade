\* ThreadCreationLive checks that retries carry every requested child through pending to ready.
SPECIFICATION LiveSpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Calls <- ModelCalls
  Threads <- ModelThreads
  Target <- ModelTarget
  None = "none"
PROPERTY AllRequestsReady
