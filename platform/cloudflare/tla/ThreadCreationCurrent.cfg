\* ThreadCreationCurrent must violate ReadyHasAccepted when the directory publishes before child acceptance.
SPECIFICATION CurrentSpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Calls <- ModelCalls
  Threads <- ModelThreads
  Target <- ModelTarget
  None = "none"
INVARIANT TypeOK
INVARIANT ReadyHasAccepted
