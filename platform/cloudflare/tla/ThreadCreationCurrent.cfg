\* ThreadCreationCurrent must violate CreatedHasAccepted when the actor publishes before child acceptance.
SPECIFICATION CurrentSpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Calls <- ModelCalls
  Threads <- ModelThreads
  Target <- ModelTarget
  None = "none"
INVARIANT TypeOK
INVARIANT CreatedHasAccepted
