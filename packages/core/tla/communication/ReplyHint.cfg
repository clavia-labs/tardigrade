\* ReplyHint is expected to fail when an independent target disagrees with the accepted inbound source.
SPECIFICATION HintSpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Addresses <- ModelAddresses
  Requests <- ModelRequests
  RequestLink <- ModelRequestLink
  TerminalReports <- ModelTerminalReports
INVARIANT ReplyReversesAcceptedLink
