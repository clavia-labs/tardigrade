\* ReplyLive checks that every finished request eventually delivers its derived reply.
SPECIFICATION LiveSpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Addresses <- ModelAddresses
  Requests <- ModelRequests
  RequestLink <- ModelRequestLink
  TerminalReports <- ModelTerminalReports
PROPERTY AllFinishedReply
