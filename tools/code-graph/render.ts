import type { GraphData } from "./types"

// renderGraph embeds a navigable dependency snapshot without external assets.
export const renderGraph = (data: GraphData): string => {
  const serialized = JSON.stringify(data).replace(/</g, "\\u003c").replace(/\u2028/g, "\\u2028").replace(/\u2029/g, "\\u2029")
  return String.raw`<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Tardigrade architecture explorer</title>
<style>
:root{color-scheme:light dark;font:13px/1.5 system-ui,sans-serif;background:var(--bg);color:var(--ink);--bg:#f8f7f2;--surface:#fffefa;--ink:#101521;--muted:#526073;--line:#2450ff59;--accent:#2450ff;--wash:#edf2ff}
@media(prefers-color-scheme:dark){:root{--bg:#11151c;--surface:#181d26;--ink:#edf0f5;--muted:#abb4c2;--line:#8fa3ff59;--accent:#8fa3ff;--wash:#222b43}}
*{box-sizing:border-box}body{margin:0}button,input{font:inherit;color:inherit}button{cursor:pointer;background:transparent;border:1px solid var(--line);border-radius:0;padding:6px 10px}button:hover{background:var(--wash)}button:active{background:var(--wash)}button:focus-visible,input:focus-visible,summary:focus-visible{outline:2px solid var(--accent);outline-offset:3px}button[aria-pressed=true]{color:var(--accent)}h1{font-size:14px;font-weight:600;margin:0;white-space:nowrap}h2{font-size:15px;margin:0 0 8px}h3{font-size:12px;font-weight:500;color:var(--muted);margin:22px 0 8px}p{margin:6px 0 12px}small,.muted{color:var(--muted)}header{height:65px;display:flex;align-items:center;gap:24px;padding:0 24px;border-bottom:1px dashed var(--line)}.search-wrap{position:relative;width:300px;margin-right:auto}input[type=search]{width:100%;padding:8px 11px;border:1px solid var(--line);border-radius:0;background:transparent}#search-results{position:absolute;top:42px;left:0;width:400px;max-width:90vw;background:var(--surface);border:1px solid var(--line);border-radius:0;padding:8px;z-index:3}#search-results[hidden]{display:none}#files{max-height:60vh;overflow:auto}.file{display:block;width:100%;text-align:left;border:0;padding:6px 8px;font-size:12px;overflow-wrap:anywhere}.file.active{color:var(--accent)}.file small{display:none}.package-head{font-size:11px;padding:8px}.count,#status{color:var(--muted);font-size:11px}#status{padding:8px}.options{position:relative}.options summary{cursor:pointer;color:var(--muted);list-style:none}.options summary::-webkit-details-marker{display:none}.options-body{position:absolute;right:0;top:30px;width:270px;background:var(--surface);border:1px solid var(--line);border-radius:0;padding:16px;z-index:4}.options-body label{display:flex;align-items:center;gap:8px;margin-bottom:12px}.meta{font-size:11px;color:var(--muted);margin-top:16px}main{padding:24px}#content{display:grid;grid-template-columns:minmax(0,1fr);gap:28px}#content.has-detail{grid-template-columns:minmax(0,1fr) minmax(330px,36%)}.graph-scroll{overflow:auto}svg{display:block;width:100%;min-width:600px}svg .node{cursor:pointer}svg .node rect{stroke-width:1}svg .node:hover rect,svg .node:focus rect{stroke:var(--accent);stroke-width:2}svg .edge{stroke:var(--accent);fill:none;opacity:.22}svg .edge.focus{stroke:var(--accent);opacity:.85}svg .node.dim{opacity:.3}svg text{font-family:ui-monospace,SFMono-Regular,monospace;pointer-events:none}.graph-hint{color:var(--muted);font-size:11px;margin:0 14px 18px}.detail{border-left:1px solid var(--line);padding-left:24px;min-width:0;max-height:calc(100vh - 115px);overflow:auto}.detail-close{float:right;margin:0 0 8px 12px}.panel{margin-bottom:20px}table{width:100%;border-collapse:collapse;font-size:11px}th{text-align:left;color:var(--muted);font-weight:500}td,th{padding:8px 4px;border-bottom:1px solid var(--line);vertical-align:top;overflow-wrap:anywhere}.link{padding:0;border:0;background:none;color:var(--accent);text-align:left;overflow-wrap:anywhere}.link:hover{background:none;text-decoration:underline}.badge{font-size:10px;color:var(--muted)}.two{display:grid;grid-template-columns:1fr;gap:4px}.source{position:relative;overflow:auto;max-height:500px;background:var(--surface);border:1px solid var(--line);border-radius:0;padding:12px 0;font:11px/1.7 ui-monospace,monospace;tab-size:2}.source-line{display:block;white-space:pre;min-width:max-content;padding:0 12px}.source-line.selected{background:var(--wash)}.ln{display:inline-block;width:36px;color:var(--muted);user-select:none}.crumb{font-size:12px;display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px}.wrap{overflow-wrap:anywhere}.warning{border-left:2px solid var(--accent);padding:8px 12px;margin:10px 0}.list-row{padding:7px 0;border-bottom:1px solid var(--line)}details{margin:12px 0}summary{cursor:pointer;color:var(--accent)}code{font:11px ui-monospace,monospace}.audit{max-width:800px;margin:auto}
@media(max-width:950px){#content.has-detail{grid-template-columns:1fr}.detail{border-left:0;border-top:1px solid var(--line);padding:24px 0;max-height:none}header{gap:12px;padding:0 16px}main{padding:16px}.search-wrap{width:auto;flex:1}}
</style></head><body>
<header><div class="search-wrap"><input id="search" type="search" placeholder="Find a file…" aria-label="Find a file" autocomplete="off"><div id="search-results" hidden><div id="files"></div><div id="status" aria-live="polite"></div></div></div><button id="reset">Overview</button><details class="options"><summary>Options</summary><div class="options-body"><label><input id="tests" type="checkbox"> Include tests</label><label><input id="types" type="checkbox" checked> Include type imports</label><button data-view="issues">Diagnostics</button><p class="meta" id="meta"></p></div></details></header>
<main><div id="content"></div></main>
<script id="graph-data" type="application/json">` + serialized + String.raw`</script>
<script>
"use strict";
const data=JSON.parse(document.getElementById('graph-data').textContent);
const el=id=>document.getElementById(id), esc=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const nodes=new Map(data.nodes.map(n=>[n.id,n])), packages=new Map(data.packages.map(p=>[p.id,p]));
const palette=['var(--line)'];
const layers=[...new Set(data.nodes.map(n=>n.layer))].sort();
const colors=new Map(layers.map((l,i)=>[l,palette[i%palette.length]]));
let state={view:'overview',file:null,pkg:null,line:null,tests:false,types:true,query:''};
const activeNodes=()=>data.nodes.filter(n=>state.tests||!n.test);
const activeEdges=()=>data.edges.filter(e=>(state.types||!e.typeOnly)&&nodes.has(e.source)&&nodes.has(e.target)&&(state.tests||(!nodes.get(e.source).test&&!nodes.get(e.target).test)));
const color=layer=>colors.get(layer)||'var(--line)';
function button(label,kind,id,line){return '<button class="link" data-'+kind+'="'+esc(id)+'"'+(line?' data-line="'+line+'"':'')+'>'+esc(label)+'</button>'}
function packageName(id){const p=packages.get(id);return p?p.name:id}
function selectFile(id,line){state.file=id;state.line=line||null;state.pkg=nodes.get(id)?.package||null;state.view='overview';state.query='';el('search').value='';render()}
function selectPackage(id){state.pkg=state.pkg===id?null:id;state.file=null;state.line=null;state.view='overview';state.query='';el('search').value='';render()}
function fileExplorer(){
 el('search-results').hidden=!state.query.trim();
 const all=activeNodes(), matching=all.filter(n=>(n.id+' '+packageName(n.package)).toLowerCase().includes(state.query.toLowerCase()));
 const grouped=new Map();for(const n of matching){if(!grouped.has(n.package))grouped.set(n.package,[]);grouped.get(n.package).push(n)}
 el('files').innerHTML=[...grouped].sort(([a],[b])=>a.localeCompare(b)).map(([p,list])=>'<div class="package-head" style="--color:'+color(list[0].layer)+'">'+button(p,'pkg',p)+' <span class="count">'+list.length+'</span></div>'+list.sort((a,b)=>a.id.localeCompare(b.id)).map(n=>'<button class="file'+(state.file===n.id?' active':'')+'" data-file="'+esc(n.id)+'">'+esc(n.id.startsWith(p+'/')?n.id.slice(p.length+1):n.id)+'<small>'+n.lines+' lines'+(n.test?' · test':'')+'</small></button>').join('')).join('');
 el('status').textContent=matching.length+' of '+all.length+' visible files';
}
function packageGraph(ns,edges){
 const ids=[...new Set(ns.map(n=>n.package))].sort(), groups=new Map();
 for(const id of ids){const layer=packages.get(id)?.layer||ns.find(n=>n.package===id).layer;if(!groups.has(layer))groups.set(layer,[]);groups.get(layer).push(id)}
 const preferred=['application','app','apps','facade','public','platform','http','model','client','host','agent','code','core','channels','ui','tooling','tool','tools','test','tests'];
 const order=[...groups.keys()].sort((a,b)=>{let x=preferred.indexOf(a),y=preferred.indexOf(b);return(x<0?99:x)-(y<0?99:y)||a.localeCompare(b)});
 const cols=Math.min(4,Math.max(1,order.length)), colWidth=244, positions=new Map(), colHeights=Array(cols).fill(44), headings=[];
 order.forEach((layer,i)=>{const col=i%cols,top=colHeights[col];headings.push('<text x="'+(col*colWidth+18)+'" y="'+top+'" fill="'+color(layer)+'" font-size="12">'+esc(layer.toUpperCase())+'</text>');groups.get(layer).forEach((id,j)=>positions.set(id,{x:col*colWidth+14,y:top+16+j*78}));colHeights[col]+=groups.get(layer).length*78+52});
 const aggregated=new Map();for(const e of edges){const a=nodes.get(e.source).package,b=nodes.get(e.target).package;if(a===b)continue;const key=a+'\0'+b;if(!aggregated.has(key))aggregated.set(key,{a,b,count:0});aggregated.get(key).count++}
 const neighbors=new Set([state.pkg]);for(const e of aggregated.values())if(e.a===state.pkg||e.b===state.pkg){neighbors.add(e.a);neighbors.add(e.b)}
 const lines=[...aggregated.values()].sort((a,b)=>Number(a.a===state.pkg||a.b===state.pkg)-Number(b.a===state.pkg||b.b===state.pkg)).map(e=>{const a=positions.get(e.a),b=positions.get(e.b);if(!a||!b)return '';const same=a.x===b.x,forward=b.x>a.x;const x1=a.x+(same?210:forward?210:0),x2=b.x+(same?210:forward?0:210),y1=a.y+27,y2=b.y+27;const bend=same?Math.max(x1,x2)+28:(x1+x2)/2;return '<path class="edge'+(state.pkg&&(e.a===state.pkg||e.b===state.pkg)?' focus':'')+'" d="M'+x1+','+y1+' C'+bend+','+y1+' '+bend+','+y2+' '+x2+','+y2+'" stroke-width="'+Math.min(1.25,0.5+Math.log2(e.count+1)/10)+'" marker-end="url(#arrow)"><title>'+esc(e.a+' → '+e.b+': '+e.count+' imports/exports')+'</title></path>'}).join('');
 const boxes=ids.map(id=>{const p=positions.get(id),fileCount=ns.filter(n=>n.package===id).length,layer=packages.get(id)?.layer||ns.find(n=>n.package===id).layer,label=id.length>29?'…'+id.slice(-28):id;return '<g class="node'+(state.pkg&&!neighbors.has(id)?' dim':'')+'" tabindex="0" role="button" aria-label="'+esc('Inspect '+id)+'" data-pkg="'+esc(id)+'"><rect x="'+p.x+'" y="'+p.y+'" width="210" height="54" rx="0" fill="'+(state.pkg===id?'var(--wash)':'var(--surface)')+'" stroke="'+color(layer)+'"/><text x="'+(p.x+11)+'" y="'+(p.y+23)+'" fill="var(--ink)" font-size="12">'+esc(label)+'</text><text x="'+(p.x+11)+'" y="'+(p.y+41)+'" fill="var(--muted)" font-size="10">'+fileCount+' files</text><title>'+esc(packageName(id))+'</title></g>'}).join('');
 return '<div class="graph-scroll"><svg viewBox="0 0 '+(cols*colWidth+35)+' '+Math.max(...colHeights)+'" role="img" aria-label="Package dependencies. Arrows point from importer to dependency."><defs><marker id="arrow" markerWidth="5" markerHeight="5" refX="5" refY="2.5" orient="auto"><path d="M0 0 L5 2.5 L0 5" fill="var(--accent)"/></marker></defs>'+headings.join('')+lines+boxes+'</svg></div>';
}
function edgeTable(edges,direction){
 if(!edges.length)return '<p class="muted">None with current filters.</p>';
 return '<table><thead><tr><th>'+(direction==='incoming'?'Importer':'Dependency')+'</th><th>Evidence</th><th>Kind</th></tr></thead><tbody>'+edges.map(e=>'<tr><td>'+button(direction==='incoming'?e.source:e.target,'file',direction==='incoming'?e.source:e.target)+'</td><td>'+button(e.specifier+' · line '+e.line,'file',e.source,e.line)+'</td><td><span class="badge">'+e.kind+(e.typeOnly?' · type':'')+'</span></td></tr>').join('')+'</tbody></table>';
}
function fileDetail(edges){const n=nodes.get(state.file);if(!n)return '<div class="panel"><h2>Source unavailable</h2><p>'+esc(state.file)+'</p><p class="muted">This diagnostic references a path outside the embedded source snapshot.</p></div>';const outgoing=edges.filter(e=>e.source===n.id),incoming=edges.filter(e=>e.target===n.id);
 return '<div class="panel"><div class="crumb">'+button(n.package,'pkg',n.package)+'<span>/</span><span class="wrap">'+esc(n.id)+'</span></div><p class="muted">'+esc(n.layer)+' · '+n.lines+' lines · '+outgoing.length+' outgoing edges · '+incoming.length+' incoming edges'+(n.test?' · test file':'')+'</p><div class="two"><section><h3>Imports and re-exports</h3>'+edgeTable(outgoing,'outgoing')+'</section><section><h3>Imported by</h3>'+edgeTable(incoming,'incoming')+'</section></div>'+(n.external.length?'<details><summary>External specifiers ('+n.external.length+')</summary><p class="wrap"><code>'+n.external.map(esc).join('<br>')+'</code></p></details>':'')+'<h3>Source snapshot</h3><p class="muted">Select an evidence link to highlight its import line.</p><div class="source" tabindex="0" aria-label="Source code">'+n.source.split('\n').map((line,i)=>'<span id="source-'+(i+1)+'" class="source-line'+(state.line===i+1?' selected':'')+'"><span class="ln">'+(i+1)+'</span>'+esc(line)+'</span>').join('')+'</div></div>';
}
function packageDetail(ns,edges){const id=state.pkg,p=packages.get(id),mine=ns.filter(n=>n.package===id),out=edges.filter(e=>nodes.get(e.source).package===id&&nodes.get(e.target).package!==id),inc=edges.filter(e=>nodes.get(e.target).package===id&&nodes.get(e.source).package!==id);
 const counts=(list,key)=>{const map=new Map();for(const e of list){const pkg=nodes.get(e[key]).package;map.set(pkg,(map.get(pkg)||0)+1)}return [...map].sort((a,b)=>b[1]-a[1]).map(([pkg,count])=>'<div class="list-row">'+button(pkg,'pkg',pkg)+' <span class="count">'+count+' edges</span></div>').join('')||'<p class="muted">None</p>'};
 return '<div class="panel"><h2>'+esc(id)+'</h2><p class="muted">'+esc(p?.name||id)+' · '+mine.length+' files · '+mine.reduce((s,n)=>s+n.lines,0).toLocaleString()+' lines</p><div class="two"><section><h3>Depends on</h3>'+counts(out,'target')+'</section><section><h3>Used by</h3>'+counts(inc,'source')+'</section></div><details><summary>Files ('+mine.length+')</summary>'+mine.map(n=>'<div class="list-row">'+button(n.id.slice(id.length+1),'file',n.id)+'</div>').join('')+'</details><details><summary>Manifest dependencies</summary><div class="two"><section><h3>Production</h3><code>'+((p?.dependencies||[]).map(esc).join('<br>')||'None')+'</code></section><section><h3>Development</h3><code>'+((p?.devDependencies||[]).map(esc).join('<br>')||'None')+'</code></section></div><p class="muted">The graph shows resolved source imports. Manifest declarations are listed separately.</p></details><details><summary>Cross-package import evidence ('+(out.length+inc.length)+')</summary><h3>Outgoing</h3>'+edgeTable(out,'outgoing')+'<h3>Incoming</h3>'+edgeTable(inc,'incoming')+'</details></div>';
}
function issues(ns,edges){const visible=new Set(ns.map(n=>n.id)),violations=data.violations.filter(v=>(!nodes.has(v.source)||visible.has(v.source))&&(!nodes.has(v.target)||visible.has(v.target)));
 const cycles=data.cycles.filter(c=>c.every(id=>!nodes.has(id)||visible.has(id)));
 return '<div class="panel"><h2>Boundary findings</h2><p class="muted">'+violations.length+' findings from the analyzer. Findings use file visibility; type-only filtering changes the map and edge lists, not this recorded audit.</p>'+violations.map(v=>'<div class="warning"><b>'+esc(v.rule)+'</b><p>'+esc(v.message)+'</p>'+button(v.source+':'+v.line,'file',v.source,v.line)+' → '+(nodes.has(v.target)?button(v.target,'file',v.target):esc(v.target))+'</div>').join('')+(!violations.length?'<p>No recorded findings for visible files.</p>':'')+'</div><div class="panel"><h2>Dependency cycles</h2><p class="muted">'+cycles.length+' recorded cycle groups. A group lists mutually reachable files or packages; list order does not imply a direct edge between adjacent entries. Groups are computed from production runtime imports. Test and type-only edges are excluded.</p>'+cycles.map((c,i)=>'<details><summary>Group '+(i+1)+' · '+c.length+' members</summary>'+c.map(id=>'<div class="list-row">'+(nodes.has(id)?button(id,'file',id):button(id,'pkg',id))+'</div>').join('')+'</details>').join('')+(!cycles.length?'<p>No recorded cycles for visible files.</p>':'')+'</div><div class="panel"><h2>Unresolved imports</h2><p class="muted">'+data.unresolved.length+' specifiers could not be mapped to an internal file. Review these before treating the graph as complete.</p>'+data.unresolved.filter(u=>visible.has(u.file)).map(u=>'<div class="list-row">'+button(u.file+':'+u.line,'file',u.file,u.line)+'<br><code>'+esc(u.specifier)+'</code></div>').join('')+'</div>';
}
function render(){const ns=activeNodes(),edges=activeEdges();
 fileExplorer();
 el('content').classList.toggle('has-detail',state.view==='overview'&&Boolean(state.file||state.pkg));
 if(state.view==='issues')el('content').innerHTML='<section class="audit">'+issues(ns,edges)+'</section>';
 else el('content').innerHTML='<section>'+packageGraph(ns,edges)+'</section>'+((state.file||state.pkg)?'<section class="detail"><button class="detail-close" data-view="close" aria-label="Close details">Close</button>'+(state.file?fileDetail(edges):packageDetail(ns,edges))+'</section>':'');
 if(state.line){const line=el('source-'+state.line);if(line){const source=line.parentElement;source.scrollTop=Math.max(0,line.offsetTop-source.offsetTop-120)}}
}
el('meta').textContent='Snapshot '+data.generatedAt+' · commit '+data.commit;
el('tests').addEventListener('change',e=>{state.tests=e.target.checked;if(state.file&&nodes.get(state.file)?.test&&!state.tests){state.file=null;state.line=null}render()});
el('types').addEventListener('change',e=>{state.types=e.target.checked;render()});
el('search').addEventListener('input',e=>{state.query=e.target.value;fileExplorer()});
el('reset').addEventListener('click',()=>{state.file=null;state.pkg=null;state.line=null;state.view='overview';state.query='';el('search').value='';render()});
document.addEventListener('click',e=>{const b=e.target.closest('[data-file],[data-pkg],[data-view]');if(!b)return;if(b.dataset.file)selectFile(b.dataset.file,Number(b.dataset.line)||null);else if(b.dataset.pkg)selectPackage(b.dataset.pkg);else{if(b.dataset.view==='close'){state.file=null;state.pkg=null;state.line=null;state.view='overview'}else state.view=b.dataset.view;document.querySelector('.options').open=false;render()}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){state.query='';el('search').value='';el('search-results').hidden=true;document.querySelector('.options').open=false}const b=e.target.closest('g[data-pkg]');if(b&&(e.key==='Enter'||e.key===' ')){e.preventDefault();selectPackage(b.dataset.pkg)}});
render();
</script></body></html>`
}
